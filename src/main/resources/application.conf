ktor {
  deployment {
    port = 8080
    watch = [ classes ]
  }
  application {
    modules = [ com.example.ApplicationKt.module ]
  }
}

# Use DATABASE_URL env var (jdbc:postgresql://host:port/db?user=...&password=...)
